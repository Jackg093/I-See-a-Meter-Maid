<p id="notice"><%= notice %></p>
<%= link_to 'New Location', new_location_path, class: "large-btn" %>
<h1 id="RSL">Recently Searched Locations</h1>

<!--
<%= form_tag locations_path, :method => :get do %>
  <p>
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Search Near", :name => nil %>
  </p>
<% end %>
 -->

<table>
  <thead>
    <tr>
      <th>Address</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th colspan="3">Options</th>
    </tr>
  </thead>

  <tbody>
    <% @locations.each do |location| %>
      <tr>
        <td><%= location.address %></td>
        <td><%= location.latitude %></td>
        <td><%= location.longitude %></td>
        <td><%= link_to 'Show', location %></td>
        <td><%= link_to 'Edit', edit_location_path(location) %></td>
        <td><%= link_to 'Destroy', location, method: :delete, data: { confirm: 'Are you sure you want to delete this location?' } %></td>
      </tr>
    <% end %>
  </tbody>

</table>

<br>

     
  <!-- this button (on this page) doesn't do anything right now, but I want it accessable from most pages -->
<h1>
 <button id="maptrigger">Find My Location! </button> 
</h1>



